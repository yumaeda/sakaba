function generateShopItemHtml(shops) {
    var html = ''
    shops.forEach(function(shop) {
        html += '<li class="shop-item">'
        html +=     '<h4>'
        html +=         '<a href="' + shop.url + '" rel="nofollow" target="_blank">' + shop.name + '</a>'
        html +=     '</h4>'
        html +=     '<p>'
        html +=         '<span>' + shop.address + '</span><br>'
        html +=         '<span>' + shop.open_hours + '</span><br>'
        html +=         '<a href="tel:' + shop.tel + '">' + shop.tel + '</a>'
        html +=     '</p>'
        html += '</li>'
    })

    return html
}
